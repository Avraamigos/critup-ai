// Shared font stacks for the polish-pass design rules.
// MONO: every numeral, counter, and micro-label — gives scores and stats a
// precise, technical read (the one element approved from the design pitches).
export const MONO = "ui-monospace, 'SF Mono', Menlo, Consolas, monospace"
